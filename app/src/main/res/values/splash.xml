<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.App.Starting" parent="Theme.AppCompat.NoActionBar">

        <item name="windowSplashScreenBackground">#FFEB3B</item>

        <item name="android:windowBackground">@drawable/ic_splash_screen</item>

        <!--color bottom navigation-->
        <item name="android:navigationBarColor">@color/white</item>

        <!--desactivado para que sea blanco el color del top bar-->
        <item name="android:windowLightStatusBar">true</item>

        <item name="postSplashScreenTheme">@style/Theme.Ecommerce</item>

        <!--color del topBar del splash screen-->
        <item name="android:statusBarColor">@android:color/white</item>

    </style>
</resources>